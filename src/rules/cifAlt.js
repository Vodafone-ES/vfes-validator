/**
 * Validación de CIF
 */
/*
// str -> str
const toUpperCase = cif => cif.toUpperCase();

// str -> boolean
const validFormat = cif => !/^[A-Z][0-9]{7}[A-Z]$/g.test(cif);

const letters = ['J', 'A', 'B', 'C', 'D', 'E', 'F', 'G', 'H', 'I'];
const digits = cif => cif.substr(1, cif.length - 2);
const letter = cif => cif.substr(0, 1);
const control = cif => cif.substr(cif.length - 1);
*/
